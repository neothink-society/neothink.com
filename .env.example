# Copy to `.env.local` for local scripts (never commit `.env.local`).
# WordPress Application Password: Users → your user → Application Passwords.
# Use the password without spaces when pasting into WORDPRESS_APPLICATION_PASSWORD.

WORDPRESS_BASE_URL=https://neothink.com
WORDPRESS_USERNAME=
WORDPRESS_APPLICATION_PASSWORD=
